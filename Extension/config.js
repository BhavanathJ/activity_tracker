// IMPORTANT: Change this to "brave" or "edge" if installing in those browsers.
export const BROWSER_ID = "brave";
export const SERVICE_PORT = 14321;
export const CONFIG_URL = `http://127.0.0.1:${SERVICE_PORT}/config`;
export const EVENT_URL = `http://127.0.0.1:${SERVICE_PORT}/event`;
